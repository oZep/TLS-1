using System;
using UnityEngine;

public class SpawnBulletPattern : MonoBehaviour
{
    int start_index;

    public class Spawn
    {
        
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
